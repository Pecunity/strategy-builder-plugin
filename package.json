{
  "name": "strategy-builder-plugin",
  "version": "1.0.0",
  "description": "This is a basic example of an ERC-6900 compatible plugin called CounterPlugin, built in Foundry. It has one function that can be called through a user operation, called `increment`. In `/src` you will find this plugin, documented so you can understand how it works in detail.",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "test": "forge test",
    "compile": "hardhat compile && forge build",
    "coverage": "forge coverage --report lcov && genhtml lcov.info --branch-coverage --output-dir coverage",
    "deploy": "hardhat deploy"
  },
  "keywords": [],
  "author": "3Blocks",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-foundry": "^1.1.3",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.18",
    "hardhat-deploy": "^0.14.0",
    "ts-node": "^10.9.2",
    "typechain": "^8.3.2",
    "typescript": "^5.7.3"
  }
}
